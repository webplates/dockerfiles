language: generic

sudo: false

matrix:
    include:
        - env: IMAGE=zero

script:
    - if .travis/build-condition.sh $TRAVIS_COMMIT_RANGE $IMAGE; then export BUILD=true; else export BUILD=false; fi
    - .travis/build.sh $IMAGE $BUILD
